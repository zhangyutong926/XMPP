using System.Collections;
using System.Collections.Generic;
using UnityEngine;

class XMPPDieRotation {
    List<Quaternion> d20 = new List<Quaternion> {
        Quaternion.identity,
        // Quaternion.Euler(),
    };
}

public class XMPPDie : MonoBehaviour {
    // Start is called before the first frame update
    void Start() {
        // var r1 = new Rotation
    }

    // Update is called once per frame
    void Update() {

    }
}
